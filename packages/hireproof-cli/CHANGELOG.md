# Changelog

## 1.0.1 - 2026-06-11

- Published the CLI with `engines.node` set to `>=22.0.0` to match Ink 7.
- Normalized npm repository metadata to `git+https://github.com/Iron-Mark/Hackathon-HireProof.git`.
- Verified `npx @hireproof/cli@latest --help` from a clean temporary folder.
